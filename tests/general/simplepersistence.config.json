{
  "workflows": [
    {
      "name": "simple persistence",
      "steps" : [
        {
          "stepId": "perist",
          "type": "scripted",
          "resultsProcessor": {
            "script": "context.setPersistedItem('myItem', 'theValue'); context.diagnosticLog(context.getPersistedItem('myItem'));"
          },
          "trigger" : {
            "runOnce": true
          },
          "persistenceFile": "persistenceFile.data"
        }
      ]
    }
  ]
}