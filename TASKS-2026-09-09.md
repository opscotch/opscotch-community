# Task list — 2026-09-09

Add new requested work here before implementation.

## Pending

- [ ] Add a chart visibility search that filters displayed selected charts without deselecting them or stopping their data collection.
- [ ] Redesign the metric criteria and filtering UX using the consolidated requirements in `CRITERIA-UX-REDESIGN.md`, including persistence of complete criteria configuration.



## Completed

- [x] Detect deployment identity exclusively from `dimensionMap.deploymentId` for series/chart grouping; top-level and `dimensions` locations are not valid metric sources.
- [x] Replace the stale-chart “Waiting for a fresh sample” text with a small accessible hourglass overlay in the chart’s top-right corner, without changing layout.

- [x] Group selected charts by `deploymentId`, with a clear deployment heading and a fallback group for metrics without one.

- [x] Preserve a selected chart’s historical line when its metric becomes stale, ending it at the final sample and leaving a visible gap to the advancing current time instead of hiding the entire chart trace.

- [x] Keep selected charts’ time axes advancing when their metrics stop arriving, and suppress a stale metric line instead of rendering an outdated value as live.

- [x] Allow manual criteria field paths and typed values even when they are not yet in the catalogue, so saved filters (for example `dimensions.error is true`) match future metrics when they arrive.
- [x] Re-evaluate active metric-browser criteria as the catalogue receives new metrics, using a responsiveness-friendly bounded/debounced cadence so newly occurring field values (for example `error=true`) appear in results.

- [x] Key live metric series and catalogue entries by metric name plus `deploymentId`, so same-named metrics from different deployments remain independently chartable and filterable.
- [x] Make the Name/string `contains` text input match the criteria picker sizing and responsive layout.

- [x] Replace the criteria field/value dropdowns with a scalable searchable picker that supports metric names and field-value filtering without rendering an unmanageable option list.
- [x] Keep metric-browser criteria dropdowns and inputs focused while live metric updates arrive, so selection remains usable during streaming.
- [x] Consolidate metric-value comparisons into the shared criteria field selector, replacing the separate Metric value editor.

- [x] Refine the criteria builder: show only one Name field option; offer `contains` for string-valued fields; and make `is`/`is not` use the indexed-value dropdown while `contains` uses a free-text value input.
- [x] Add criteria groups with OR semantics, so multiple values for a field can match any value in the group while groups remain composable with the existing filtering.

- [x] Fix the metric field-value dropdown option label so it renders as `value (count)` without a trailing `}`.
- [x] Add a metric-name “contains” operation to the shared field criteria builder, using a text value input rather than an indexed-value dropdown.

- [x] Open the metric search panel immediately, then populate/refresh its records after debounced filtering completes so opening remains responsive for large catalogues.
- [x] Diagnose and fix the metric-browser “Select filtered (N)” bulk action so it reliably selects matching metrics up to the configured chart cap.
- [x] Replace the layout-shifting field-filter feedback message with a brief glow/flash on the clicked filter-in or filter-out button.
- [x] Present the metric search browser as a fast open/close overlay (rather than expanding page layout) to minimize reflow for large catalogues.

- [x] When text is entered in the metric search box, represent and apply it as a visible “name contains X” search criterion.
- [x] Prioritize browser responsiveness for large metric streams: debounce search/filter evaluation and throttle or batch chart/UI updates so incoming metric frequency cannot degrade interaction responsiveness.

- [x] Add a configurable maximum chart-selection cap to the metric browser, defaulting to 200 charts.
- [x] When a filter is added from a displayed metric field, apply it without opening the metric browser/search panel.
- [x] Show visual feedback when a filter change is applied from a metric-field selector.

- [x] Add a “filter out” action to field pills in metric-search results, alongside the existing action that filters for the field/value.
- [x] Make filter-in and filter-out actions visually distinct: green for inclusion and red for exclusion.
- [x] Add numeric metric-value filters with comparison operators such as equals, greater than, and less than.

- [x] When a filter is added from a metric sample (result field chip or metric-body modal), immediately apply it by opening/updating the metric browser rather than requiring a separate search interaction.
- [x] Make field/filter pills within each metric-search result compact and allow multiple pills per line instead of stretching across the full result width.

- [x] Add a preset filter option for metrics with a non-zero value.
- [x] Make active filter pills compact to their content rather than filling horizontal space.
- [x] Replace the “View latest metric body” text button with a discrete icon action, retaining an accessible label.
- [x] Add a clear-selection button beside the “N charts selected” indicator.
- [x] Rename the metric-browser bulk action “Select none” to “Clear”.
