{
  "comment": "Key-store request validation workflow without a storage dependency.",
  "workflows": [
    {
      "name": "key-store validation test",
      "steps": [
        {
          "stepId": "accept-key-store",
          "trigger": {
            "deploymentAccess": {
              "ids": ["key-store-call"]
            }
          },
          "resultsProcessor": {
            "resource": "/accept-key-store.js"
          }
        },
        {
          "stepId": "testTrigger",
          "resultsProcessor": {
            "script": "function expectError(request) { var response = context.sendToStep('accept-key-store', JSON.stringify(request)); if (!response.isErrored()) throw new Error('expected request rejection'); } expectError({}); expectError({ get: { keyId: '' } }); expectError({ get: { keyId: 'service/example' } }); expectError({ getOrGenerate: { keyId: 'service/example' } }); expectError({ getOrGenerate: { keyId: 'service/example', purpose: 'unsupported' } }); expectError({ put: { keyId: 'service/example', value: 'AQ==' } }); expectError({ get: { keyId: 'service/example', purpose: 'sign' }, getOrGenerate: { keyId: 'service/example', purpose: 'sign' } }); context.sendMetric('key-store-validation-pass', 1.0);"
          }
        }
      ]
    }
  ]
}
