{
  "workflows": [
    {
      "name": "file reading",
      "steps" : [
        {
          "stepId": "fileReader",
          "type": "scripted",
          "trigger": {
            "fileWatcher" : {
              "bootstrapFileId": "testFile",
              "eventSplitter": "\\n"
            }
          },
          "resultsProcessor": {
            "script": "console.log(context.getBody());JSON.parse(context.getBody()).forEach(l => context.diagnosticLog(l.message))"
          }
        }
      ]
    }
  ]
}